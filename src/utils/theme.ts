import { createMuiTheme } from "@material-ui/core/styles";

export const themeLight = createMuiTheme({
  palette: {
    background: {
      default: "#fff",
    },
    primary: {
      main: "#393a3b",
    },
    secondary: {
      main: "#d7dbd9",
    },
  },
});
